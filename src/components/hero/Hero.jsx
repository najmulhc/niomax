 
import { H2, P } from '../shared/typography'
import Button from '../shared/Button'
import Container from '../shared/Container'
 
const Hero = () => {
  return (
    <Container className=' min-h-screen bg-gradient '>
      
      <Button href="#menu">explore menu</Button>
    </Container>
  )
}

export default Hero